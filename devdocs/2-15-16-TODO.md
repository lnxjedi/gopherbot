# 2-15 TODO

## Primary Work

- Shift focus from strict backward compatibility to a clear upgrade path via `UPGRADING-v3.md` in the repository root.
- Continue the "new robot setup and development workflow" epic with fast local iteration.
- Remove the setuid helper created for testing; it is no longer part of the plan. (Completed)
- Add `DefaultProtocol` (alongside `PrimaryProtocol`) so default message delivery can differ from control protocol.
- Move protocol configs to `conf/protocols/<protocol>.yaml` for clearer structure and parity with plugin config layout.
- Introduce explicit environments for robot startup (`GOPHER_ENVIRONMENT`) and drive mode-specific behavior from environment-specific config includes.
- Do a focused audit/discussion of startup modes and mode detection (including retirement path for legacy `setup`/`test-dev`/`ide-override` behavior).
- Remove legacy file-brain git backup/restore behavior (including `.restore`-style markers and related save/restore coupling) via a short research spike first, then a dedicated code-removal story.

## New-Robot Setup Direction

- During initial setup, generate a permanent SSH server key for the robot.
- Store the encrypted server key in protocol config (for example `conf/protocols/ssh.yaml`) and publish the public key at `custom/robot-ssh.pub`.
- Update `bot-ssh` behavior to prefer `custom/robot-ssh.pub` when present.
- In the new-robot workflow, prompt for an admin username (recommended to match expected default chat identity).
- In the new-robot workflow, prompt for an admin SSH public key.
- End state: user can immediately connect with `bot-ssh -l <username>`, and that first user is the initial admin.

## Stretch Goal

- Rewrite help to avoid flooding users with commands they cannot run.
- Update plugin help config format so help output is naturally linked to command definitions.
- Extend authorizer plugin spec with a `usergroups` command that returns a JSON array of group names for a username.
- Use this group data to filter `;help` output per user permissions (for example `usergroups david` drives what David sees).

## Later TODO

- SSH connector identity mapping should support distinguishing users by full SSH public-key identity including the trailing key comment (not just key type + key blob), so dev/test users can share one keypair while mapping to different usernames/roles.
