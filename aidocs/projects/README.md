# Active Projects

- `user-docs-refresh/README.md` — co-locate the mdBook manual, reorganize it
  around DevOps operator concerns, bring each concern current, and establish
  the keep-docs-current contract. Resume from
  `user-docs-refresh/STATUS.md`.

Project records in this directory are temporary. Remove the project directory
and its index entry when the project's documented exit criteria are complete.

For a resumed project, treat its STATUS.md file as the handoff authority after
verifying it against `git status` and the current source tree.
