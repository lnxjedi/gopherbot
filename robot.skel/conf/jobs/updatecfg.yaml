---
# Custom configuration for the updatecfg job, overrides/augments
# conf/jobs/updatecfg.yaml from the distribution.
HistoryLogs: 7
Quiet: true # The reload plugin provides feedback
Channel: general
# Example trigger. If GOPHER_CUSTOM_REPOSITORY is set, this trigger can
# be used for the robot to update it's configuration with `git pull` and
# reload. That way, updating configuration for your robot is as simple
# as `git push`.
#Triggers:
#- User: github
#  Channel: <FIXME>
#  Regex: 'new commit.*github.com\/<FIXME>\/<YOUR-ROBOT-REPO>\/tree'
