Until other plugins are developed, Gopherbot only supports a file-based
brain for storing it's memories. Be sure <config dir>/brain exists
and is writable by the user the robot runs as. (or <install dir>/brain
if not using a config dir)
