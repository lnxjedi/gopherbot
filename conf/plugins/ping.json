{
	"comment": "These are used to see if the robot is alive, so should answer in every channel",
	"AllChannels": true,
	"Channels": [ ],
	"Help": [
		{
			"Keywords": [ "ping", "beep" ],
			"Helptext": [ "(bot), ping - see if the bot is alive", "(bot), beep - see if the bot can hear you" ]
		},
		{
			"Keywords": [ "rules" ],
			"Helptext": [ "(bot), what are the rules? - Be sure the robot knows how to conduct his/herself." ]
		}
	],
	"comment": "Note that backslashes need to be escaped as double backslashes, e.g. \\",
	"CommandMatches": [
		{
			"Command": "ping",
			"Regex": "ping"
		},
		{
			"Command": "thanks",
			"Regex": "(?i:thanks?( you)?!?)"
		},
		{
			"Command": "rules",
			"Regex": "(?i:(?:what are )?the rules\\??)"
		},
		{
			"Command": "beep",
			"Regex": "beep"
		}
	]
}
