{
	"PluginPath": "plugins/external/echo",
	"comment": "AllowDirect determines whether the plugin responds to direct messages",
	"AllowDirect": true,
	"Channels": [ "botdev" ],
	"Help": [
		{
			"Keywords": [ "echo" ],
			"Helptext": [ "(bot), echo <simple text> - trivially repeat a phrase" ]
		}
	],
	"comment": "Note that backslashes need to be escaped as double backslashes, e.g. \\",
	"CommandMatches": [
		{
			"Command": "echo",
			"Regex": "echo ([.;!\\d\\w-, ]+)"
		}
	]
}
