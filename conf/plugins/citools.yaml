---
# Plugin configuration for citools
Help:
- Keywords: [ "build" ]
  Helptext:
  - "(bot), build <repo> (branch) (pipeline) (args) - start a gopherci build for the given repo"
  - "(bot), help with build - give detailed help on the build command"
CommandMatchers:
- Command: 'build'
  Regex: '(?i:build ([^\s]+)(?: ([^\s]+))?(?: ([^\s]+))?(?: (.*))?)'
- Command: 'help'
  Regex: '(?i:help with build(?:ing)?)'
