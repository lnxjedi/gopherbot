{
	"AdminContact": "",
	"comment": "If you specify default channels, plugins without a channel list will only respond in these channels",
	"DefaultChannels": [ "general", "random" ],
	"IgnoreUsers": [],
	"JoinChannels": [ "random", "general" ],
	"AdminUsers": [],
	"Alias": ";",
	"LocalPort": "8880",
	"comment": "LogLevel one of trace, debug, info, warn, error",
	"LogLevel": "info",
	"comment": "ExternalPlugins will cause the bot to look for these extra <pluginname>.json files. If it's not listed here it won't be active",
	"ExternalPlugins": [
		"hosts",
		"echo",
		"whoami"
	],
	"Protocol": "slack",
	"ProtocolConfig": {
		"SlackToken": "<your_token_here>",
		"comment": "MaxMessageSplit specifies the maximum number of messages to break a message into when it's too long (>4000 char)",
		"MaxMessageSplit": 2
	},
	"comment": "How to store plugin and plugin:user data",
	"Brain": "file",
	"BrainConfig": {
		"comment": "For a file-storage based brain, just store files in a directory. Relative paths are relative to the local config dir.",
		"BrainDirectory": "brain"
	}
}
